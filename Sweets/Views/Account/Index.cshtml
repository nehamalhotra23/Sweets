@{
  Layout = "_Layout";
}

<h2>Authentication with Identity</h2>
<hr />
@if (User.Identity.IsAuthenticated)
{
    <p>Hello @User.Identity.Name!</p>
     @using (Html.BeginForm("LogOff", "Account"))
    {
        <input type="submit" class="btn btn-primary" value="Log out" />
        
    }<hr>
    <p>@Html.ActionLink("Add new Flavour", "Create", "Flavour")</p><hr>
    <p>@Html.ActionLink("Add new Treat", "Create", "Treat")</p><hr>
    <p>@Html.ActionLink("See all flavour", "Details", "Treat")</p><hr> 
}
else
{
    <p>@Html.ActionLink("Register", "Register")</p>
    <p>@Html.ActionLink("Log in", "Login")</p>
     <p>@Html.ActionLink("See all flavour", "Index", "Treat")</p>  
}